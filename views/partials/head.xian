<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="MBC Bongabong Campus Clinic — student health services, consultations, and medicine dispensing.">
  <link rel="icon" href="/favicon.ico" />
  <script src="https://cdn.tailwindcss.com"></script>
  <!-- DataTables CSS -->
  <link rel="stylesheet" href="https://cdn.datatables.net/1.13.6/css/jquery.dataTables.min.css" />
  <title>MBC Bongabong Campus Clinic - {{title}}</title>
  <style>
    /* small page-level tweaks */
    :root { --brand: #0ea5a4; --brand-600: #059669; --brand-700: #047857; --sidebar-width: 16rem; --sidebar-collapsed-width: 4rem; }
    html,body { height: 100%; }
    body { background-color: #f8fafc; font-family: Inter, ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial; }
    .hero-gradient { background: linear-gradient(90deg,var(--brand) 0%, var(--brand-600) 100%);} 

  /* sidebar link helpers (plain CSS for runtime) */
  .sidebar-link { display: flex; align-items: center; gap: 0.5rem; padding: 0.5rem 0.75rem; border-radius: 0.375rem; color: inherit; text-decoration: none; }
  .sidebar-link:hover { background-color: #ecfeff; }
  .sidebar-link.active, .sidebar-link[aria-current="true"] { background-color: #ccfbf1; color: #0f766e; font-weight: 600; }

  /* Sidebar sizing and responsive behaviour */
  .site-sidebar { width: var(--sidebar-width); transition: width 200ms ease; }
  .site-sidebar.collapsed { width: var(--sidebar-collapsed-width); overflow: hidden; }

  /* For pages that shift content when sidebar is visible.
    Shift the inner centered container (.max-w-7xl) instead of relying on global
    padding so the centered layout stays predictable. */
  .main-shift { transition: padding-left 200ms ease; padding-left: 0; }
  .main-shift.collapsed { padding-left: 0; }

  /* On md+ screens the sidebar is in the document flow (see sidebar partial's
    md:static/md:block). Do not double-shift the inner container — let the
    sidebar's width determine layout naturally. */

  /* Defensive override: ensure the sidebar participates in layout on md+ even
     if Tailwind utility ordering differs. Keep in-flow when NOT collapsed and
     allow .collapsed to reduce width using its own rule. */
  @media (min-width: 768px) {
    .site-sidebar:not(.collapsed) { position: static !important; transform: none !important; inset: auto !important; width: var(--sidebar-width) !important; display: block !important; }
    .site-sidebar.collapsed { width: var(--sidebar-collapsed-width) !important; overflow: hidden !important; }
    /* make sure the sidebar doesn't overlap content and is full height */
    .site-sidebar { height: auto; }
  }

  /* Primary brand helpers */
  .btn-brand { background-color: var(--brand); color: white; }
  .btn-brand:hover { background-color: var(--brand-700); }

    /* responsive main container tweaks */
    .app-container { max-width: 1200px; margin-left: auto; margin-right: auto; }
    /* reserve space on small screens for the fixed bottom nav so content isn't obscured */
    @media (max-width: 767px) {
      body { padding-bottom: 4.25rem; }
    }

    /* Simple dark mode support via a 'dark' class on the <body>.
       This provides minimal overrides so components using bg-white/text-gray
       still have reasonable contrast when dark mode is enabled. Stored in
       localStorage under 'siteTheme' as 'dark' or 'light'. */
    body.dark { background-color: #0b1220; color: #e6eef6; }
    body.dark .bg-white { background-color: #0f1724 !important; }
    body.dark .text-gray-600 { color: #94a3b8 !important; }
    body.dark .text-gray-500 { color: #94a3b8 !important; }
    body.dark .border { border-color: rgba(255,255,255,0.06) !important; }
    body.dark .shadow-md { box-shadow: 0 1px 3px rgba(2,6,23,0.6) !important; }
    body.dark .sidebar-link:hover { background-color: rgba(16,24,39,0.6); }
    body.dark .sidebar-link { color: #cbd5e1; }
  </style>
</head>